# Iteration 2

This folder now contains iteration-2 artifacts and documentation only.

The project source code has been consolidated into the repository-level `src/` folder so there is a single canonical implementation for the full system.
